<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="FControl.Pages.DisplaySettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Padding="24,16,24,24" Spacing="20">
            <StackPanel Spacing="4">
                <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="显示设置" />
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="配置提示浮层外观与常用调节步进。M3 已接入亮度/音量调节步进，浮层外观设置将在 M4 完成。" />
            </StackPanel>

            <Border
                Padding="16"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
                <StackPanel Spacing="18">
                    <StackPanel Spacing="6">
                        <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="提示浮层持续时间" />
                        <Slider
                            Header="1–10 秒"
                            Maximum="10"
                            Minimum="1"
                            SmallChange="0.5"
                            StepFrequency="0.5"
                            Value="3" />
                    </StackPanel>

                    <StackPanel Spacing="6">
                        <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="提示浮层透明度" />
                        <Slider
                            Header="20%–100%"
                            Maximum="100"
                            Minimum="20"
                            SmallChange="5"
                            StepFrequency="5"
                            Value="80" />
                    </StackPanel>

                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <NumberBox
                            x:Name="BrightnessStepNumberBox"
                            Header="亮度调节步进（%）"
                            Maximum="25"
                            Minimum="1"
                            SmallChange="1"
                            SpinButtonPlacementMode="Inline"
                            Value="5"
                            ValueChanged="ControlStepNumberBox_ValueChanged" />
                        <NumberBox
                            x:Name="VolumeStepNumberBox"
                            Grid.Column="1"
                            Header="音量调节步进（%）"
                            Maximum="25"
                            Minimum="1"
                            SmallChange="1"
                            SpinButtonPlacementMode="Inline"
                            Value="2"
                            ValueChanged="ControlStepNumberBox_ValueChanged" />
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
