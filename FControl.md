# **FControl** — 需求规格说明书

## 1. 文档概述
本文档旨在定义一款 Windows 桌面工具 **“F键控制（FControl）”** 的功能需求、交互体验、技术架构与设计规范。该工具允许用户通过 F1–F12 功能键（可自定义）快速调节系统音量、屏幕亮度以及控制媒体播放，同时支持用户配置组合快捷键执行本地自定义脚本，并以 Fluent Design 风格提供直观的视觉反馈。

---

## 2. 项目背景与目标
许多键盘（尤其是紧凑布局或机械键盘）缺少独立的多媒体控制键，用户调整音量、亮度或播放控制时需频繁使用鼠标操作任务栏或物理按钮。本工具的目标是：
- 将 F1–F12 复用为专用媒体与系统控制键，支持自定义映射。
- 支持 `Ctrl` / `Alt` / `Shift` / `Win` 等组合快捷键触发用户自定义脚本，例如 `Ctrl + Shift + K` 执行一段 Python、Node.js、Bash、PowerShell 或 Windows Shell 命令。
- 提供显示器 DDC/CI 能力检测、Python / Node.js 运行环境检测与路径配置，降低用户配置成本。
- 通过绚丽的 Fluent 浮层提示给予即时的视觉反馈。
- 提供清爽、现代的设置界面，让所有层级用户都能轻松配置。
- 兼容 Windows 10 / 11，覆盖 x64 与 x86 系统。

---

## 3. 核心功能需求

### 3.1 功能键映射控制
| 功能类别 | 操作      | 描述                          |
| -------- | --------- | ----------------------------- |
| 屏幕亮度 | 减小亮度  | 通过 DDC/CI 以步进值降低 HDMI/DP 外接显示器亮度 |
|          | 增大亮度  | 通过 DDC/CI 以步进值提高 HDMI/DP 外接显示器亮度 |
| 音量控制 | 音量减小  | 逐步降低系统主音量            |
|          | 音量增大  | 逐步提高系统主音量            |
|          | 静音切换  | 切换系统静音状态（开/关）     |
| 媒体播放 | 回退      | 媒体回退指定秒数（默认2秒）   |
|          | 暂停/播放 | 切换媒体播放/暂停状态         |
|          | 快进      | 媒体快进指定秒数（默认2秒）   |
| 自定义脚本 | 执行脚本/命令 | 通过用户配置的组合快捷键执行本地 Shell、PowerShell、Bash、Python、Node.js 脚本或命令 |

> **默认按键分配**：参见 [7. 默认按键映射](#7-默认按键映射)。

### 3.2 自定义按键映射
- 用户可为每个 **F1–F12** 自由分配如下动作：
  - 亮度调节（增大/减小）
  - 音量调节（增大/减小/静音）
  - 媒体控制（播放/暂停、快进、回退、下一曲、上一曲、停止）
  - 禁用（不执行任何操作）
- 媒体回退/快进的 **步进值** 可配置（例如 2 秒、5 秒、10 秒）。
- 自定义配置应持久化存储，工具启动时自动加载。

### 3.3 调节提示浮层（Toast/Overlay）
系统在执行按键操作时，屏幕 **左上角** 将出现半透明提示层，提供即时反馈：

- **亮度/音量调节**：出现水平 `Slider`，实时显示当前级别百分比，滑块随按键连续调节而动态变化。
- **静音/媒体状态**：出现对应状态图标（如静音、播放、暂停等）。
- **动画效果**：
  - **渐现（Fade In）**：200 毫秒内透明度从 0% 到 80%。
  - **持续显示**：默认停留 3 秒。
  - **渐隐（Fade Out）**：500 毫秒内透明度从 80% 到 0%，随后控件销毁。
- 持续时间允许用户在设置中修改（1–10 秒）。
- 连续快速按键时，重新计时（显示时间刷新）。

### 3.4 自定义快捷键与脚本执行
用户可在新增的 **“快捷键功能”** 页面中新增、编辑、删除自定义快捷键动作，使任意受支持组合键触发本地脚本或命令。

#### 3.4.1 快捷键规则
- 支持组合键：`Ctrl`、`Alt`、`Shift`、`Win` + 字母 / 数字 / 功能键 / 常用特殊键。
- 示例：`Ctrl + Shift + K`、`Alt + F9`、`Win + Ctrl + B`。
- 不允许配置系统保留或不可拦截快捷键，例如 `Ctrl + Alt + Del`、`Win + L`、`Alt + Tab`。
- 快捷键默认全局生效，即应用在托盘后台运行时仍可触发。
- 可为每条快捷键单独设置启用 / 禁用。

#### 3.4.2 脚本类型
每个自定义快捷键可选择以下执行类型：

| 类型 | 执行方式 | 说明 |
| ---- | -------- | ---- |
| Windows Shell | `cmd.exe /c` | 执行 `.bat`、`.cmd` 或单行命令 |
| PowerShell | `powershell.exe` 或 `pwsh.exe` | 执行 `.ps1` 文件或 PowerShell 命令；默认不绕过系统执行策略 |
| Bash | `bash.exe -lc` | 支持 Git Bash、WSL Bash 或用户自定义 Bash 路径 |
| Python | `python.exe` / `py.exe` / 用户自定义路径 | 执行 `.py` 文件或内联 Python 代码，前提是本机已安装 Python |
| Node.js | `node.exe` / 用户自定义路径 | 执行 `.js` 文件或内联 JavaScript 代码，前提是本机已安装 Node.js |
| 外部程序 | 可执行文件路径 + 参数 | 用于启动任意本地工具或脚本解释器 |

#### 3.4.3 快捷键配置字段
新增 / 编辑快捷键时至少包含：
- 名称：便于用户识别，例如“打开开发环境”“切换代理”“运行备份脚本”。
- 快捷键：通过录入框捕获组合键。
- 动作类型：内置动作或自定义脚本。
- 脚本类型：Shell / PowerShell / Bash / Python / Node.js / 外部程序。
- 执行内容：脚本文件路径或内联脚本代码。
- 参数：可选命令行参数。
- 工作目录：可选，默认使用应用配置目录或脚本所在目录。
- 运行方式：隐藏窗口 / 普通窗口 / 最小化窗口。
- 超时时间：默认 30 秒，可配置；超时后提示用户并可终止进程。
- 并发策略：上一次仍在运行时，可选择“忽略本次触发”“允许并发运行”“终止上一次后重新运行”。
- 执行反馈：是否显示浮层提示、是否记录日志、是否显示失败通知。

#### 3.4.4 保存与冲突检测
点击保存时必须进行快捷键冲突检测：
- 检测应用内部重复：自定义快捷键之间不可重复。
- 检测与 F1–F12 内置按键映射冲突：同一按键或组合键不能同时绑定多个启用动作。
- 检测系统保留快捷键：对已知无法注册或高风险快捷键给出明确提示。
- 检测系统级占用：通过临时 `RegisterHotKey` 试注册验证是否已被其他程序占用；失败时提示“该快捷键可能已被系统或其他应用占用”。
- 冲突处理策略：用户可取消保存、修改快捷键，或保存为禁用状态；启用状态下不得保存冲突快捷键。

#### 3.4.5 执行与安全要求
- 脚本执行必须异步进行，不阻塞 UI 线程和全局热键监听。
- 捕获退出码、标准输出、标准错误，并保留最近执行记录，便于排查。
- 默认不请求管理员权限；如脚本需要管理员权限，应由用户自行配置外部启动方式。
- 首次启用自定义脚本功能时显示安全提示：脚本拥有当前用户权限，请仅运行可信代码。
- 日志中不得记录用户主动标记为敏感的参数或环境变量。
- 提供“测试运行”按钮，保存前可验证脚本路径、解释器路径、退出码与输出。

---

## 4. 用户界面设计

### 4.1 设计语言
严格遵循 **Fluent Design System**：
- 亚克力背景材质、阴影、圆角、流畅动画。
- 色彩使用系统 Accent Color 适配用户主题（浅色/深色）。

### 4.2 布局结构 — NavigationView
主窗口采用 `NavigationView` 实现侧边导航布局（左侧汉堡菜单/图标 + 右侧内容区域）。

导航项规划：
1. **按键映射**（默认页）  
   - 列表形式显示 F1–F12，每项旁边有下拉框选择动作。
   - 针对“回退/快进”动作，显示秒数输入框。
   - 提供“恢复默认”按钮。
2. **快捷键功能**  
   - 列表形式展示用户新增的组合快捷键：启用状态、名称、快捷键、动作类型、脚本类型、最近执行结果。
   - 提供“新增快捷键”“编辑”“删除”“复制”“测试运行”操作。
   - 新增 / 编辑弹窗支持录入组合键、选择脚本类型、填写脚本内容 / 文件路径 / 参数 / 工作目录。
   - 保存时实时展示冲突检测结果；存在冲突时阻止启用。
3. **设置**  
   - **显示与浮层**：提示浮层持续时间滑块（1–10 秒，步长0.5）、透明度强度、亮度 / 音量调节步进值。
   - **显示器检测**：检测并展示外接显示器 DDC/CI 支持状态，提供“重新检测”按钮；检测失败时显示“请检查显示器是否支持 DDC/CI 功能，并确认显示器菜单中已开启 DDC/CI；转接线、扩展坞、KVM 或显卡驱动也可能导致不可用”。
   - **脚本运行环境**：检测 Python、Node.js 环境，显示版本号与自动检测路径；旁边提供自定义路径输入框、浏览按钮、测试按钮、恢复自动检测按钮。
   - 可选扩展检测 Bash / Git Bash / WSL / PowerShell Core 路径。
4. **关于**  
   - 应用名称、版本号、GitHub 链接（如有）、致谢 Fluent UI System Icons 等。

### 4.3 窗口行为
- 设置窗口为普通窗口，可最小化、最大化、关闭。
- 关闭窗口后应用转入 **系统托盘** 继续后台运行，拦截全局热键。
- 托盘图标右键菜单包含：打开设置、退出。
- 首次启动自动打开设置界面。

---

## 5. 技术实现要求

### 5.1 技术栈
- **UI 框架**：WinUI 3 + Windows App SDK (1.5+)
- **语言**：C# / .NET 8 或 C++/WinRT（按团队技术路线）
- **打包方式**：MSIX 打包（便于自动更新与干净卸载），同时提供 sideload 安装包。
- **图标集**：**Fluent UI System Icons** （图标样式匹配系统原生控件）

### 5.2 兼容性
- **操作系统**：Windows 10 版本 1809（Build 17763）及以上、Windows 11 全部版本。
- **架构**：x64、x86。ARM64 可选支持。
- **权限**：
  - 媒体控制、音量调节：使用 `Windows.Media.Playback` 或发送模拟按键（`VK_MEDIA_*`），无需管理员权限。
  - **屏幕亮度**：优先通过 **DDC/CI** 控制 HDMI/DP 外接显示器亮度，使用 Windows Monitor Configuration API（如 `EnumDisplayMonitors`、`GetPhysicalMonitorsFromHMONITOR`、`GetMonitorBrightness`、`SetMonitorBrightness`、`DestroyPhysicalMonitors`）。若显示器、线材、扩展坞、KVM 或显卡驱动不支持 DDC/CI，应给出明确提示；内置屏亮度控制可作为可选降级能力，不作为主要目标。

### 5.3 全局热键
- 通过 `RegisterHotKey` 或 `KeyboardAccelerator` 结合低等级键盘钩子确保全系统（即使窗口未聚焦）均可捕获 F1–F12 与用户自定义组合快捷键（需避免与其他软件冲突，可选“以管理员身份运行”可提升成功率）。
- 对自定义组合快捷键优先使用 `RegisterHotKey`，便于系统级冲突检测；如后续需要支持更复杂按键序列，可再引入低级键盘钩子作为扩展。
- 热键注册失败时应记录 Win32 错误码，并在 UI 中给出可理解提示。

### 5.4 脚本执行服务
- 新增 `ScriptExecutionService`，统一负责构造命令、启动进程、超时控制、输出捕获、退出码记录和错误提示。
- 使用 `ProcessStartInfo` 执行脚本：默认 `UseShellExecute = false`，根据用户设置决定是否创建窗口。
- 按脚本类型自动拼接解释器参数，例如：
  - Shell：`cmd.exe /c <command>`
  - PowerShell：`powershell.exe -NoProfile -File <script.ps1>` 或 `pwsh.exe -NoProfile -File <script.ps1>`
  - Bash：`bash.exe -lc <command>`
  - Python：`python.exe <script.py>` 或临时文件承载内联代码
  - Node.js：`node.exe <script.js>` 或临时文件承载内联代码
- 内联脚本建议写入应用临时目录后执行，避免复杂转义导致执行错误；执行后按策略清理临时文件。
- 执行结果写入最近运行记录：触发时间、快捷键、脚本名称、退出码、耗时、stdout / stderr 摘要。

### 5.5 运行环境检测
- 新增 `RuntimeEnvironmentService`，用于检测解释器路径、版本与可用性。
- Python 检测顺序建议：用户自定义路径 → `py -0p` / `py -3` → `python --version` → `python3 --version` → `where python`。
- Node.js 检测顺序建议：用户自定义路径 → `node --version` → `where node`。
- Bash 检测可选：用户自定义路径 → `where bash` → Git Bash 默认路径 → `wsl.exe`。
- PowerShell 检测可选：系统 Windows PowerShell → `pwsh.exe`。
- 检测结果包含：是否可用、版本号、可执行文件路径、错误信息、最后检测时间。
- 用户自定义路径保存后必须执行一次 `--version` 或等价命令验证；验证失败时提示但允许保存为禁用 / 未验证状态。

### 5.6 DDC/CI 检测
- 通过 Windows Monitor Configuration API 枚举物理显示器，并尝试读取亮度能力判断 DDC/CI 可用性。
- 检测结果按显示器展示：显示器名称、连接序号、是否支持亮度读取、是否支持亮度写入、当前亮度、错误信息。
- 对不支持或检测失败的显示器显示排查建议：请检查显示器是否支持 DDC/CI、OSD 菜单是否已开启 DDC/CI、连接方式是否为 HDMI/DP、转接线 / 扩展坞 / KVM / 显卡驱动是否支持。
- 必须确保所有物理显示器句柄在检测后正确释放。

### 5.7 配置与数据模型
- 配置应包含版本号，便于后续迁移。
- 建议配置结构示例：

```json
{
  "version": 2,
  "keyMappings": {
    "F1": { "action": "BrightnessDown" },
    "F2": { "action": "BrightnessUp" }
  },
  "customHotkeys": [
    {
      "id": "uuid",
      "enabled": true,
      "name": "Run custom Python script",
      "hotkey": "Ctrl+Shift+K",
      "actionType": "Script",
      "scriptType": "Python",
      "scriptMode": "File",
      "scriptPath": "D:/scripts/demo.py",
      "inlineCode": null,
      "arguments": "",
      "workingDirectory": "D:/scripts",
      "runWindowMode": "Hidden",
      "timeoutSeconds": 30,
      "concurrencyPolicy": "IgnoreIfRunning",
      "showOverlay": true
    }
  ],
  "runtimePaths": {
    "python": "",
    "node": "",
    "bash": ""
  }
}
```

---

## 6. 非功能性需求
- **性能**：内存占用 < 80 MB，CPU 空闲时近 0%。
- **响应**：按键到触发系统操作延迟 < 50 ms，浮层显示延迟 < 30 ms。
- **稳定性**：连续 24 小时运行无崩溃、无内存泄漏。
- **易用性**：安装后首次运行引导设置可选按键；设置界面所有选项含清晰标签与工具提示。
- **可访问性**：支持高对比度、文本缩放，可通过 Tab 键遍历所有控件。

---

## 7. 默认按键映射

| 功能键 | 默认动作      | 说明          |
| ------ | ------------- | ------------- |
| F1     | 屏幕亮度减小  | 步进默认 5%   |
| F2     | 屏幕亮度增大  | 步进默认 5%   |
| F3     | （未分配）    | 可自定义      |
| F4     | （未分配）    | 可自定义      |
| F5     | （未分配）    | 可自定义      |
| F6     | （未分配）    | 可自定义      |
| F7     | 媒体回退      | 默认回退 2 秒 |
| F8     | 媒体暂停/播放 | 切换播放状态  |
| F9     | 媒体快进      | 默认快进 2 秒 |
| F10    | 静音切换      | 开/关         |
| F11    | 音量减小      | 步进默认 2%   |
| F12    | 音量增大      | 步进默认 2%   |

> 未分配的 F3–F6 出厂置为“禁用”，用户可按需开启。

### 7.1 默认自定义快捷键
- 默认不预置任何会执行脚本的自定义快捷键，避免安装后产生不可预期行为。
- 用户可自行新增，例如 `Ctrl + Shift + K` → 执行指定 Python / Node.js / Bash / Shell 脚本。
- 首次创建脚本快捷键时应显示安全提示并要求用户确认。

---

## 8. 项目里程碑（建议）

### M0：技术预研与方案确认
**目标**：验证关键技术可行性，降低后续开发风险。

**交付内容**：
- 确认技术路线：WinUI 3 + Windows App SDK + .NET 8。
- 验证全局热键捕获方案：`RegisterHotKey` 或低级键盘钩子。
- 验证自定义组合快捷键注册、冲突检测与脚本执行方案。
- 验证音量、媒体控制、DDC/CI 外接显示器亮度控制 API 的可用性。
- 验证 Python、Node.js、Bash 等运行环境检测方案。
- 确认 MSIX 打包、系统托盘、开机自启动的实现方式。

**验收标准**：
- 能在后台捕获至少一个 F 键与一个组合快捷键并触发测试逻辑。
- 能成功调用音量或媒体控制能力。
- 能执行一个本地测试脚本并捕获退出码、stdout、stderr。
- 明确 DDC/CI 在 HDMI/DP 外接显示器、转接线、扩展坞、KVM、多显示器环境下的兼容边界。

---

### M1：应用基础框架
**目标**：完成应用主框架与基础运行能力。

**交付内容**：
- 搭建 WinUI 3 桌面应用工程。
- 实现 `NavigationView` 主界面框架。
- 完成“按键映射”“快捷键功能”“设置”“关于”四个页面的基础布局。
- 实现窗口关闭后进入系统托盘。
- 实现托盘菜单：打开设置、退出。

**验收标准**：
- 应用可正常启动、最小化、关闭到托盘。
- 托盘菜单可重新打开设置窗口并退出应用。
- 页面导航流畅，无明显 UI 卡顿。
- 快捷键功能页可完成新增 / 编辑 / 删除表单交互原型。

---

### M2：热键与配置系统
**目标**：实现 F1–F12 与自定义组合快捷键的映射配置、冲突检测与持久化。

**交付内容**：
- 实现 F1–F12 全局热键注册与冲突处理。
- 实现自定义组合快捷键注册、试注册冲突检测、系统保留快捷键校验。
- 实现默认按键映射。
- 实现按键动作枚举：亮度、音量、静音、播放/暂停、快进、回退、禁用等。
- 实现配置文件读写、版本迁移与启动自动加载。
- 实现自定义快捷键的新增、编辑、删除、启用、禁用。
- 实现“恢复默认”功能。

**验收标准**：
- 默认映射启动后自动生效。
- 用户修改映射后，重启应用仍能保留配置。
- 禁用的按键不会触发任何操作。
- 热键注册失败时能给出明确提示。
- 保存自定义快捷键时可阻止重复、保留键和系统占用冲突。

---

### M3：核心控制能力
**目标**：完成音量、媒体播放、亮度控制等核心功能。

**交付内容**：
- 实现系统音量增大、减小、静音切换。
- 实现媒体播放/暂停、上一曲、下一曲、停止。
- 实现媒体快进/回退，支持自定义秒数。
- 实现通过 DDC/CI 对 HDMI/DP 外接显示器进行亮度增大、减小。
- 枚举可控制的物理显示器，并正确释放显示器句柄。
- 实现 DDC/CI 支持状态检测与错误提示。
- 实现自定义脚本执行服务，支持 Shell、PowerShell、Bash、Python、Node.js。
- 对不支持 DDC/CI 或无法读取/写入亮度的设备给出降级提示。

**验收标准**：
- F10–F12 默认音量控制可正常工作。
- F7–F9 默认媒体控制可正常工作。
- F1–F2 默认亮度控制可在支持 DDC/CI 的 HDMI/DP 外接显示器上正常工作。
- 不支持的功能不会导致应用崩溃。
- 自定义脚本可异步执行，能记录退出码、耗时和输出摘要。

---

### M4：Fluent 浮层与设置完善
**目标**：完成主要用户体验与视觉反馈。

**交付内容**：
- 实现左上角半透明提示浮层。
- 音量/亮度调节时显示 Slider 与百分比。
- 静音、播放、暂停、快进、回退等动作显示对应图标。
- 实现渐现、停留、渐隐动画。
- 支持配置浮层显示时长、透明度、调节步进值。
- 设置页展示 DDC/CI 检测状态、Python / Node.js 检测状态和自定义路径配置。
- 适配浅色/深色主题与系统 Accent Color。

**验收标准**：
- 按键触发后浮层能在 30 ms 内出现。
- 连续按键时浮层内容刷新并重新计时。
- 动画符合文档定义的时长。
- 设置项修改后立即或下次触发生效。
- Python / Node.js 自定义路径保存前可测试，测试结果清晰展示。

---

### M5：测试、兼容性与稳定性
**目标**：完成发布前质量保障。

**交付内容**：
- 测试 Windows 10 / Windows 11 兼容性。
- 测试 x64、x86 架构。
- 测试多显示器、外接显示器、不同媒体播放器场景。
- 测试连续按键、高频触发、长时间后台运行。
- 测试自定义脚本超时、失败、并发触发、路径不存在、解释器不存在等异常场景。
- 测试 DDC/CI 不支持、多显示器、显示器休眠 / 唤醒后的检测表现。
- 完成异常处理与日志记录。
- 完成可访问性检查：Tab 导航、高对比度、文本缩放。

**验收标准**：
- 连续运行 24 小时无崩溃。
- 空闲 CPU 接近 0%，内存占用低于 80 MB。
- 高频按键不会造成明显卡顿或浮层异常。
- 关键异常有日志记录或用户可理解的提示。
- 自定义脚本执行失败不会导致应用崩溃或热键监听失效。

---

### M6：打包、发布与后续维护
**目标**：完成正式发布准备。

**交付内容**：
- 制作 MSIX 安装包。
- 配置应用图标、版本号、发布说明。
- 完成代码签名。
- 准备 GitHub Release 或官网下载页。
- 可选提交 Microsoft Store。
- 整理用户使用说明与常见问题。
- 增加自定义脚本安全说明、DDC/CI 排查说明、Python / Node.js 环境配置说明。

**验收标准**：
- 用户可通过安装包完成安装、启动、卸载。
- 卸载后不残留关键配置或后台进程。
- 发布包包含版本号、更新说明和基础文档。
- 已记录下一阶段可选功能，例如 App-Specific 配置、宏录制、脚本市场 / 模板、云同步等。

---

> 本需求文档将作为开发与验收的依据，后续如有新增功能（如支持 App-Specific 按键配置、宏录制等）需更新本文档。
